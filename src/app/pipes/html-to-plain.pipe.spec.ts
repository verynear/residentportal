import { HtmlToPlainPipe } from './html-to-plain.pipe';

describe('HtmlToPlainPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlToPlainPipe();
    expect(pipe).toBeTruthy();
  });
});
